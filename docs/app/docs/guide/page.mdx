---
asIndexPage: true
---

import {
  BrushIcon,
  FilesIcon,
  GlobeIcon,
  LightningIcon,
  LinkIcon,
  PictureIcon,
  StarsIcon
} from '@components/icons'
import { Cards } from 'nextra/components'
import { FileIcon, FolderIcon, MarkdownIcon, MdxIcon } from 'nextra/icons'
import { MDXRemote } from 'nextra/mdx-remote'
import { createIndexPage, getPageMap } from 'nextra/page-map'

# Guide

The following features are configured via the Next.js configuration and are
available in all themes.

<MDXRemote
  compiledSource={await createIndexPage(await getPageMap('/docs/guide'))}
  components={{
    Cards,
    FilesIcon,
    MarkdownIcon,
    StarsIcon,
    LinkIcon,
    GlobeIcon,
    LightningIcon,
    PictureIcon,
    BrushIcon,
    FileIcon,
    FolderIcon,
    MdxIcon
  }}
/>
