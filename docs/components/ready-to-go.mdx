### Ready to Go!

Now, you can create your first MDX page as `pages/index.mdx`:

```mdx filename="pages/index.mdx"
# Welcome to Nextra

Hello, world!
```

And run the `dev` command specified in `package.json` to start developing the
project! 🎉

```sh npm2yarn
npm run dev
```
