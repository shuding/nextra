# Search

Nextra features full-page search, that includes list items and code blocks.

- A list
- That can be searched

## Search in `<code attr="code1" />{:html}`

Inline `<code attr="code2" />{:html}` can be searched as well as clode blocks.

```html
<code>I'm Searchable!</code>
```
