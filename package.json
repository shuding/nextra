{
  "name": "nextra-monorepo",
  "private": true,
  "workspaces": [
    "packages/nextra",
    "packages/nextra-theme-docs",
    "packages/nextra-theme-blog",
    "examples/docs",
    "examples/blog"
  ],
  "scripts": {
    "build:core": "yarn workspace nextra run build",
    "build:theme-docs": "yarn workspace nextra-theme-docs run build",
    "build:theme-blog": "yarn workspace nextra-theme-blog run build",
    "version-beta": "lerna version --preid beta --force-publish",
    "publish-beta": "lerna publish from-package --canary --preid beta --pre-dist-tag beta"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.2.1"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "trailingComma": "none",
    "arrowParens": "avoid"
  },
  "version": "0.0.0"
}
